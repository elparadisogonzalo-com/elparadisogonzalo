import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.elparadisogonzalo.app',
  appName: 'elparadisogonzalo app',
  webDir: 'build'
};

export default config;
